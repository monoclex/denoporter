{
  "name": "denoporter",
  "version": "1.0.0",
  "description": "Rewrites imports to have the appropriate extension on the end when importing with deno.",
  "main": "src/index.ts",
  "scripts": {
    "publish": "wrangler publish",
    "build": "wrangler build",
    "dev": "wrangler preview --watch",
    "dev:local-no-reloading": "npm run build && cloudworker --debug worker/script.js"
  },
  "keywords": [
    "deno",
    "import",
    "typescript"
  ],
  "author": "SirJosh3917",
  "license": "MIT",
  "dependencies": {
    "typescript": "^3.9.5"
  },
  "devDependencies": {
    "@cloudflare/wrangler": "^1.9.2",
    "@dollarshaveclub/cloudworker": "^0.1.2",
    "ts-loader": "^7.0.5"
  }
}
